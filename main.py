"""Thin dev runner. The real entry point lives in the package.

Run via `uv run main.py`, or use the installed `nanoPyCodeAgent` command.
"""
from nanopycodeagent import main

if __name__ == "__main__":
    main()
